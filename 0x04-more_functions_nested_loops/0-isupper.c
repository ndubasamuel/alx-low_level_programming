#include "main.h"
#include <stdio.h>

/**
 * function _isupper - checkes for uppercase letters 
 * @c: character is the character to check
 *
 * Return: 0 or 1
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);

	return (0);

}

